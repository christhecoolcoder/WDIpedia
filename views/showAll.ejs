<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <link rel="stylesheet" href="./style.css" />  

        <title>WDIPEDIA | Students</title>
    </head>

    <body id='flexwrap'>


        <% students.forEach((skillRecord)=> { %>
          <div>
            <p id='name'>Student: <%=skillRecord.name%></p>
            <p>Skill: <%=skillRecord.skill%></p>
            <p>is good at?: <%=skillRecord.proficient%></p>
            
            <form action="/students/<%=skillRecord.id%>/edit" method="post">
              <input name="skill">    
              <button type="submit">edit skill</button>
            </form> 
            
            <form action="/students/<%=skillRecord.id%>/delete" method="post">   
              <button type="submit">delete skill</button>
            </form>

            <form action="/students/<%=skillRecord.student_id%>/new" method="post">
              <button type="submit">add skill</button>
            </form>
          </div>      
        <% }) %>
    </body>
</html>