<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <link rel="stylesheet" href="./style.css" />  

        <title>WDIPEDIA | Students</title>
    </head>

    <body id='flexwrap'>


        <% students.forEach((students)=> { %>
        <div>
          <p id='name'>Student: <%=students.name%></p>
          <p>Skill: <%=students.skill%></p>
          <p>is good at?: <%=students.proficient%></p>
          
            <form action="/students/<%=students.id%>" method="post">
              <input name="skill">    
              <button type="submit">edit skill</button>
            </form> 
          
          <form action="/students/<%=students.id%>" method="delete">   
            <button type="submit">delete skill</button> 
          </form>

          <form action="/students/<%=students.id%>" method="post">
            <button type="submit">add skill</button>
          </form>
        </div>        

        <% }) %>
    </body>
</html>